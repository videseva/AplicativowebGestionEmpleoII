import { FiltroOfertaCargoPipe } from './filtro-oferta-cargo.pipe';

describe('FiltroOfertaCargoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroOfertaCargoPipe();
    expect(pipe).toBeTruthy();
  });
});
